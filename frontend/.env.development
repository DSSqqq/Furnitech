# Режим `npm run dev`: запросы на тот же origin, Vite проксирует /api → localhost:8000.
# Оставьте пустым (или удалите строку), чтобы не ходить на прод даже при копировании .env из .env.example.
VITE_API_ORIGIN=
