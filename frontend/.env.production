# Продакшен-бэкенд на Render (Vite подставляет при `vite build`).
# На Vercel то же значение можно задать в Project → Environment Variables как VITE_API_ORIGIN
# (переопределит это файл, если указано в UI).
VITE_API_ORIGIN=https://furnitech-calc.onrender.com
