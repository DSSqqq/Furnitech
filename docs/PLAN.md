# Furnitech — план продукта

## Цель

Веб‑сайт «Фурнитех»: калькулятор для магазина с **админской** и **клиентской** частями.

- **Клиент**: калькулятор в духе [Modusline configurator / profiles](https://modusline.com/configurator/profiles) (подбор профилей и комплектации).
- **Админ**: база материалов для калькулятора, заказы.

## Этапы

| # | Этап | Статус |
|---|------|--------|
| 1 | Репозиторий: Django (API) + React (Vite), документация, handoff skill | **сделано (MVP)** |
| 2 | Модель данных: иерархия категорий, карточка «Общие параметры» (вкл. KZT, альт. валюты, артикул) | **сделано (MVP+)** |
| 3 | REST API: CRUD, фильтр по категории, дерево, JWT, `alt_prices` | **сделано (MVP+)** |
| 4 | Админ UI (React): папки, форма карточки, валюты, артикул | **сделано (MVP+)** |
| 4.1 | Сопутствующие материалы + ручные операции (модели, API, UI под списком, предпросчёт); миграция `0010` | **сделано** |
| 4.2 | Папки: переименовать/удалить (шестерёнка, меню, модалка, проверки в API); уникальный непустой артикул (миграция `0011`); вкладка «Общие параметры»; `is_active`; вёрстка 3 колонок на десктопе | **сделано** |
| 5 | Вкладки: контент «Доп. параметры» и «Параметры текстуры» | **сделано** |
| 6 | Раздел «Заказы» в админке | `запланировано` |
| 7 | Клиентский калькулятор (конфигуратор) | **MVP:** публичная главная `/`, шаги 1–4 для рамочного фасада, read-only для гостя; **дальше:** цены, корзина, оформление (реф. Modusline) |
| 8 | Интеграция с 1С: `external_id`, артикул, слой sync | `запланировано` |
| 9 | Аутентификация веб-админ API (JWT) | **сделано** |

## Нефункциональные требования

- **1С (задел)**: `external_id`, `last_synced_at` на сущностях; **артикул** (`article`) — для сопоставления с номенклатурой; детали в [ARCHITECTURE.md](ARCHITECTURE.md).
- **Стек**: Django + DRF, React (Vite), CORS, SQLite (dev) / PostgreSQL (prod).

## Ссылки

- Референс UI: https://modusline.com/configurator/profiles
